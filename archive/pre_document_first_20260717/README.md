# Pre-document-first archive — 2026-07-17

This archive was created during the transition to `CURRENT_FRAMEWORK`.

- `root_files/` contains the 45 files that previously occupied the workspace root, including legacy manuals, logs, ledgers, model registries, raw JSON data and package metadata.
- `retired_technical_runtime/` contains the previous `src`, `scripts`, `tests`, `templates`, `schema`, `runtime`, `outputs` and `node_modules` directories.

Nothing was deleted during the transition. These files are retained for audit, recovery and evidence only. They are not the active prediction process and must not be mixed into current performance without an explicit, documented migration.

The active framework is `..\..\CURRENT_FRAMEWORK`.

