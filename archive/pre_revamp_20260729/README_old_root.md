# Sports Research

The active framework is the document-first set in [CURRENT_FRAMEWORK](./CURRENT_FRAMEWORK/README.md).

Normal analysis now uses:

- one combined Markdown operating manual;
- one append-only Markdown prediction/results log;
- one CSV calibration ledger;
- one Markdown source registry;
- one Markdown model/simulation standard;
- dated Markdown audits.

No per-request JSON is required. Previous documents, raw data, generated outputs, and the retired executable JSON prototype are preserved under `archive/` for evidence and recovery; they are not the current workflow.

Start with `CURRENT_FRAMEWORK/README.md`, then follow `CURRENT_FRAMEWORK/combined_sports_doc_v4.md`.

